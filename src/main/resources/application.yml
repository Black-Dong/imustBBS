server:
  port: 2222
  servlet:
    context-path: /

#数据源配置
spring:
  profiles:
    active: prop #使用的配置文件是开发环境
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jmx:
    default-domain: ImustBBS